# AGENTS.md — Proposal-overview 프로젝트 총괄 지침서

> **프로젝트명**: RFP 맞춤형 목차 & 1~2페이지 요약 설계 서비스  
> **최종 목표**: RFP PDF를 분석하여 요약 보고서 + 제안서 작성 가이드를 자동 생성, 이메일로 전달  
> **주 고객**: 지원사업/입찰에 도전하는 초기 스타트업 대표

---

## 1. 프로젝트 개요

본 프로젝트는 초기 스타트업 대표가 **RFP(제안요청서) PDF**를 업로드하면,  
AI가 핵심 내용을 분석·요약하고, 사용자가 입력한 **회사 정보**를 결합하여  
**맞춤형 제안서 작성 가이드**까지 생성·이메일 전송하는 **엔드-투-엔드 서비스**입니다.

### 핵심 가치

| 가치 | 설명 |
|------|------|
| ⏱ 시간 절약 | 수십 페이지 RFP를 1~2페이지로 압축 |
| 🎯 맞춤형 가이드 | 회사 정보 기반 제안서 목차·전략 제안 |
| 📧 이메일 전달 | 분석 결과를 이메일로 즉시 수신 |

---

## 2. 서비스 흐름 (End-to-End Flow)

```
[사용자] ─── RFP PDF 업로드 + 회사정보 입력 ───▶ [웹 프론트엔드]
                                                       │
                                                       ▼
                                               [백엔드 서버]
                                                       │
                                          ┌────────────┼────────────┐
                                          ▼            ▼            ▼
                                    [PDF 파싱]   [AI API 호출]  [DB 저장]
                                          │            │
                                          ▼            ▼
                                   텍스트 추출 ──▶ 프롬프트 구성
                                                       │
                                          ┌────────────┴────────────┐
                                          ▼                         ▼
                                  [1차 산출물]                [2차 산출물]
                                  RFP 요약 보고서             제안서 작성 가이드
                                  (1~2페이지)                 (맞춤형 목차+전략)
                                          │                         │
                                          ▼                         ▼
                                      [이메일 발송 서비스]
                                          │
                                          ▼
                                   📧 사용자 이메일 수신
```

---

## 3. 기술 스택

| 레이어 | 기술 | 비고 |
|--------|------|------|
| **프론트엔드** | Next.js (React) | 사용자 입력 폼, 업로드 UI |
| **백엔드** | Next.js API Routes 또는 Express.js | REST API |
| **PDF 파싱** | pdf-parse, pdfjs-dist 등 | PDF → 텍스트 추출 |
| **AI API** | OpenAI GPT-4o / Claude API | 요약 & 가이드 생성 |
| **이메일 발송** | Resend / Nodemailer / SendGrid | 트랜잭셔널 이메일 |
| **DB** | Supabase / PostgreSQL | 사용자·요청 이력 저장 |
| **파일 저장소** | Supabase Storage / S3 | 업로드 PDF 저장 |
| **인증** | Supabase Auth / NextAuth | 사용자 인증 (선택) |

> [!NOTE]
> 기술 스택은 구현 단계에서 확정됩니다. 위 표는 권장 후보이며, 프로젝트 요구사항에 따라 조정 가능합니다.

---

## 4. 디렉토리 구조 (목표)

```
Proposal-overview/
├── AGENTS.md                  # 본 파일 — 프로젝트 총괄 지침
├── TASK_STATUS.md             # 작업 진행 상태 추적
├── .env                       # 환경변수 (API 키, DB URL 등)
├── .env.example               # 환경변수 예시 (커밋용)
├── package.json
│
├── src/
│   ├── app/                   # Next.js App Router 페이지
│   │   ├── page.tsx           # 랜딩 / 메인 업로드 페이지
│   │   ├── layout.tsx         # 공통 레이아웃
│   │   └── api/               # API Routes
│   │       ├── upload/        # PDF 업로드 엔드포인트
│   │       ├── analyze/       # RFP 분석 엔드포인트
│   │       ├── guide/         # 제안서 가이드 생성 엔드포인트
│   │       └── email/         # 이메일 발송 엔드포인트
│   │
│   ├── components/            # 재사용 UI 컴포넌트
│   │   ├── UploadForm.tsx     # PDF 업로드 폼
│   │   ├── CompanyInfoForm.tsx# 회사 정보 입력 폼
│   │   └── StatusTracker.tsx  # 분석 진행 상태 표시
│   │
│   ├── lib/                   # 핵심 비즈니스 로직
│   │   ├── pdf-parser.ts      # PDF 파싱 유틸리티
│   │   ├── ai-client.ts       # AI API 클라이언트
│   │   ├── prompt-builder.ts  # 프롬프트 템플릿 빌더
│   │   ├── email-sender.ts    # 이메일 발송 로직
│   │   └── db.ts              # DB 클라이언트
│   │
│   ├── prompts/               # AI 프롬프트 템플릿
│   │   ├── rfp-summary.md     # RFP 요약 프롬프트
│   │   └── proposal-guide.md  # 제안서 가이드 프롬프트
│   │
│   └── types/                 # TypeScript 타입 정의
│       └── index.ts
│
├── public/                    # 정적 파일
└── docs/                      # 추가 문서
    └── prompt-design.md       # 프롬프트 설계 문서
```

---

## 5. 핵심 기능 모듈

### 5.1 PDF 업로드 & 파싱

- 사용자가 RFP PDF 파일을 업로드
- 서버에서 PDF를 텍스트로 변환 (표, 목차 구조 보존 시도)
- 추출된 텍스트를 AI 분석용으로 전처리

### 5.2 AI 기반 RFP 요약 보고서 생성 (1차 산출물)

- 추출된 RFP 텍스트 + 요약 프롬프트 → AI API 호출
- **산출물 구성**:
  - RFP 핵심 요구사항 요약
  - 평가 기준 및 배점 정리
  - 제출 마감일·자격 요건 하이라이트
  - 맞춤형 목차 구조 제안
- 1~2페이지 분량으로 압축

### 5.3 제안서 작성 가이드 생성 (2차 산출물)

- RFP 요약 보고서 + 사용자 입력 회사 정보 → AI API 호출
- **사용자 입력 회사 정보**:
  - 회사명, 설립연도, 업종
  - 주요 제품/서비스
  - 팀 규모, 핵심 역량
  - 관련 실적/경험
- **산출물 구성**:
  - 제안서 목차 (RFP 맞춤)
  - 각 섹션별 작성 가이드라인
  - 강조해야 할 회사 강점 매핑
  - 주의사항 및 차별화 전략 제안

### 5.4 이메일 발송

- 1차(요약 보고서), 2차(작성 가이드) 산출물을 이메일로 전송
- HTML 포맷 이메일 + PDF 첨부 (선택)
- 발송 성공/실패 로깅

---

## 6. AI 프롬프트 설계 원칙

1. **구조화된 출력**: AI 응답은 항상 정해진 섹션 구조를 따르도록 프롬프트에 명시
2. **한국어 우선**: 모든 프롬프트와 출력은 한국어 기본
3. **분량 제한**: 요약 보고서는 1~2페이지 이내로 명시적 제한
4. **역할 부여**: "당신은 10년 경력의 제안서 컨설턴트입니다" 등 역할 지정
5. **Few-shot 예시**: 품질 향상을 위해 좋은 요약/가이드 예시 포함
6. **반복 개선**: 프롬프트 결과를 지속적으로 평가하고 TASK_STATUS.md에 개선 사항 기록

---

## 7. TASK_STATUS.md 운영 규칙

> [!IMPORTANT]
> **모든 작업 세션에서 TASK_STATUS.md를 반드시 확인하고 업데이트할 것.**

### 파일 구조

```markdown
# TASK_STATUS — 작업 현황

## ✅ 완료된 작업 (Done)
- [x] 작업 설명 — 완료일

## 🔄 진행 중인 작업 (In Progress)
- [ ] 작업 설명 — 시작일, 현재 상태

## 📋 다음 작업 (Next)
- [ ] 작업 설명 — 우선순위, 의존성

## 🐛 이슈 & 블로커 (Issues)
- 이슈 설명 — 영향도, 해결 방안

## 📝 메모 (Notes)
- 의사결정 기록, 참고사항
```

### 운영 원칙

1. **작업 시작 전**: TASK_STATUS.md를 읽고 현재 상태 파악
2. **작업 중**: 진행 상황을 실시간 반영
3. **작업 완료 후**: 완료 항목 이동 + 다음 작업 업데이트
4. **컨텍스트 유지**: 새 세션에서도 이전 작업 흐름을 즉시 이해할 수 있도록 충분한 맥락 기록
5. **이슈 추적**: 발생한 문제와 해결 과정을 반드시 기록

---

## 8. 개발 단계 (로드맵)

### Phase 1 — 기반 구축 🏗️
- [ ] 프로젝트 초기화 (Next.js, 의존성 설치)
- [ ] 환경변수 설정 (.env 구성)
- [ ] 기본 UI 레이아웃 구현
- [ ] TASK_STATUS.md 초기 생성

### Phase 2 — 핵심 기능 구현 ⚙️
- [ ] PDF 업로드 & 파싱 모듈
- [ ] AI API 연동 (요약 보고서 생성)
- [ ] 프롬프트 설계 & 테스트
- [ ] 회사 정보 입력 폼

### Phase 3 — 2차 산출물 & 이메일 📧
- [ ] 제안서 작성 가이드 생성 로직
- [ ] 이메일 발송 연동
- [ ] 이메일 템플릿 디자인

### Phase 4 — 통합 & 고도화 🚀
- [ ] 전체 플로우 통합 테스트
- [ ] 에러 핸들링 & 예외 처리
- [ ] UI/UX 개선
- [ ] 배포 (Vercel 등)

---

## 9. 에이전트 행동 규칙

### 일반 규칙

1. **한국어 사용**: 코드 주석, 커밋 메시지, 문서 모두 한국어 우선
2. **TASK_STATUS.md 필수 확인**: 모든 작업 시작/종료 시 반드시 확인 및 갱신
3. **점진적 구현**: 한 번에 전부 만들지 말고, 기능 단위로 점진적으로 구현
4. **테스트 우선**: 핵심 로직은 테스트 가능한 형태로 작성
5. **환경변수 보호**: API 키, 비밀번호 등은 절대 코드에 하드코딩 금지 → `.env` 사용

### 코드 작성 규칙

1. **TypeScript 사용**: 타입 안전성 확보
2. **에러 핸들링**: 모든 외부 API 호출, 파일 I/O에 try-catch 적용
3. **로깅**: 주요 처리 단계마다 로그 출력 (디버깅용)
4. **모듈화**: 기능별로 파일 분리, 단일 책임 원칙 준수
5. **환경변수**: `process.env`로 접근, 필수 변수 누락 시 명확한 에러 메시지

### 커밋 & 문서 규칙

1. 의미 있는 단위로 커밋
2. 변경사항은 TASK_STATUS.md에 즉시 반영
3. 프롬프트 변경 시 변경 사유와 결과 품질 변화 기록

---

## 10. 환경변수 목록

`.env` 파일에 아래 변수들을 설정합니다:

```env
# AI API
OPENAI_API_KEY=           # OpenAI API 키 (또는 사용할 AI 서비스 키)
AI_MODEL=                 # 사용할 모델명 (예: gpt-4o)

# 이메일 발송
EMAIL_API_KEY=            # 이메일 서비스 API 키 (Resend/SendGrid 등)
EMAIL_FROM=               # 발신자 이메일 주소

# 데이터베이스
DATABASE_URL=             # DB 연결 문자열

# 파일 저장소
STORAGE_URL=              # 파일 저장소 URL

# 기타
NEXT_PUBLIC_APP_URL=      # 앱 공개 URL
```

---

## 11. 참고 사항

- 이 문서는 프로젝트 진행에 따라 지속적으로 업데이트됩니다.
- 새로운 에이전트 세션이 시작될 때 이 문서를 첫 번째로 읽어야 합니다.
- 기술 스택 확정, 프롬프트 설계 고도화 등 주요 의사결정은 TASK_STATUS.md의 메모 섹션에 기록합니다.
